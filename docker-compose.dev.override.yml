version: '3'
services:
  app:
    volumes:
      - .:/var/www/mellivora
      - appconfig:/var/www/mellivora/include/config
      - composerdependencies:/var/www/mellivora/include/thirdparty/composer
  adminer:
      image: adminer
      restart: always
      ports:
        - 8080:8080
volumes:
  appconfig:
  composerdependencies: